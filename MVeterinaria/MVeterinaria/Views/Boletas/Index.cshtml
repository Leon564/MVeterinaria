@model IEnumerable<MVeterinaria.Models.Boleta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Mascota
        </th>
        <th>
           Veterinario
        </th>
        <th>
            Fecha de emision
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mascota.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Veterinario.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaEmision)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoletaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoletaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoletaId })
        </td>
    </tr>
}

</table>
